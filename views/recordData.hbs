<section class="clinician-safety-threshold">
  <h1 class="title">
    Patient
    {{patient.firstName}}
    {{patient.lastName}}'s Safety Threshold in
    {{records.recordDate}}
  </h1>
  < class="safety-threshold-table">

    <tr>
      <th> Availbility</th>
      <th> Attribute </th>
      <th> thresholdStatus </th>
      <th> Value </th>
      <th> Attribute Lower Bound </th>
      <th> Attribute Upper Bound </th>

      <th> Actual Recorded Time </th>
      <th> Comment </th>
    </tr>
    {{#each records.data}}
    {{# if success}}
<section class="success">
  <h2>Inputs are validated.</h2>
</section>

{{else}}
{{# if errors}}
<section class="errors">
<ul>
  {{# each errors}}
  <li>{{this.msg}}</li>
  {{/each}}
</ul>
</section>
{{/if}}

      <form action="/submit" method="POST">
        <tr>
          <input type="hidden" name="key" value="{{@key}}" />
          {{#ifUnrecorded this.thresholdStatus}}
            <td><input type="checkbox" name="availability" value="true" /></td>
          {{else}}
            <td>{{this.availability}}</td>
          {{/ifUnrecorded}}
          <td> {{this.fullName}} </td>
          <td> {{this.thresholdStatus}} </td>
          <td> {{this.value}} </td>
          {{#ifUnrecorded this.thresholdStatus}}
            <td><input
                type="text"
                name="minvalue"
                placeholder="Enter min value here"
                required
              /></td>
          {{else}}
            <td>{{this.minValue}}</td>
          {{/ifUnrecorded}}
          {{#ifUnrecorded this.thresholdStatus}}
            <td><input
                type="text"
                name="maxvalue"
                placeholder="Enter max value here"
                required
              /></td>
          {{else}}
            <td>{{this.maxValue}}</td>
          {{/ifUnrecorded}}

          <td> {{this.createdAt}} </td>
          <td> {{this.comment}} </td>
          {{#ifUnrecorded this.thresholdStatus}}
            <td><button type="submit" class="submit-button">Submit</button></td>
          {{/ifUnrecorded}}
        </tr>
      </form>
    {{/each}}

  </table>
  <!-- supporting message -->
  <form action="/clinician/{{patient._id}}/message" method="POST">
    <h3> Supporting message</h3>
    <input class="message-box" type="text" name="message" />
    <button type="submit" class="submit-button">OK</button>
  </form>

  <a href="/clinician/hist_rec/{{patient._id}}">
    <button class="submit-button"> View Historical Data </button>
  </a>

  <a href="/clinician">
    <button class="back-button"> Back </button>
  </a>

  <a href="/viewClinicianNotes">
    <button class="button"> View ClinicianNotes </button>
  </a>

  <a href="/enterClinicianNotes">
    <button class="button"> Enter ClinicianNotes </button>
  </a>

</section>